# v9.1 Recognition Implementation Plan

> Spec: `docs/superpowers/specs/2026-06-10-v9.1-recognition-design.md` (spec-reviewed, all findings folded).
> Driven directly via TDD (cross-cutting schema change — main loop holds the whole picture);
> whole-branch code-review gates the result.

**Goal:** 5형식 = recognition skill, not mandate. Session classifies input as a 5형식 moment or not; teaches accordingly.

---

## Chunk 1: schema + validate + mocks + record + all type consumers + v5→v6 migration (ONE green commit)

**Files:** `types/v9.ts`, `types/index.ts`, `services/validate.ts`(+test), `services/mocks.ts`(+test), `store/localStorage.ts`(+test), `store/firestoreDataStore.ts`, `services/migrateToCloud.ts`(+tests if rec shape), and every fixture/test factory that builds a `SessionPayload` or `LearningRecord`.

- [ ] **types/v9:** add `recognition`; `pattern5h: Pattern5HMeta | null`; reshape `comparison` → `{show,label,fiveH:{en,note},simpler:{en,note},betterChoice:'5h'|'simpler'}`; `pattern: {...} | null`; `assembly.blocks` stays `Block[]` (validate enforces 2..3). Export a `ComparisonSide` helper type if useful.
- [ ] **types/index:** `LearningRecord` — `schemaVersion: 4|5|6`, `pattern5hId: Pattern5HId|null`, `triggerVerb: string|null`, add `isFiveHMoment: boolean`.
- [ ] **validate.ts:** discriminated per spec §4 (recognition required; moment→pattern5h+pattern required+curated; !moment→both null; comparison betterChoice consistent; blocks 2..3 + unique ids + sequential orders + blockRoles length match).
- [ ] **mocks.ts:** add `recognition` + reshaped `comparison` to the 3 fixtures (all moment, betterChoice '5h'); add `FIX_SIMPLER` (isFiveHMoment:false, pattern5h:null, pattern:null, betterChoice:'simpler', 2 blocks). Keep `FIX_CAUSATIVE_BARE` as FIXTURES[hash('seed')] for the e2e — verify hash still lands on it (or reorder).
- [ ] **localStorage.ts:** `CURRENT_SCHEMA_VERSION=6`; `init()` add `stored==='5'` non-destructive branch (backfill `isFiveHMoment=true` on records lacking it); `'4'` chains 4→5→6; add `withRecordDefaults` applied in `getLearningRecords`.
- [ ] **firestoreDataStore.ts:** `getLearningRecords` maps through `withRecordDefaults` (records `isFiveHMoment` default true).
- [ ] **Update all SessionPayload/LearningRecord literals** in: `validate.test`(validPayload + new simpler valid + new throw cases), `learningStore.test`(mock already via vi.mock — but its inline literals? none for payload), `mocks.test`, `migrateToCloud.test`/`.emulator.test` rec() (add isFiveHMoment), `progress.test` rec(), `srsView.test`/`localStorage.test`/`firestoreDataStore.test` (Pattern unaffected; LearningRecord rec() factories add isFiveHMoment), `db.test` (none).
- [ ] **Run** `npx tsc -b` (0) · `npx vitest run` (all pass) · `npm run lint`. Iterate until green.
- [ ] **Commit:** `feat(v9.1): SessionPayload/LearningRecord recognition schema + validate + mocks + v5→v6 migration`

## Chunk 2: store branching

**Files:** `store/learningStore.ts`(+test)

- [ ] `isAssemblyCorrect`: `blockOrder.length !== payload.assembly.blocks.length`.
- [ ] `tapBlock`: lock at `>= payload.assembly.blocks.length`.
- [ ] `advanceToStep3`: build+save Pattern only when `payload.recognition.isFiveHMoment` (else skip to step3, set patternSaved true to keep idempotency).
- [ ] `complete`: record includes `isFiveHMoment`, `pattern5hId`/`triggerVerb` null when !moment, `schemaVersion:6`; `applyReview` only when moment; `track('session_complete')` adds `isFiveHMoment`, allows null pattern fields.
- [ ] Tests: add simpler-path test (mock FIX_SIMPLER via vi.mock override) → no savePattern, no applyReview, record.isFiveHMoment=false + pattern5hId null. Keep moment tests green.
- [ ] **Run** tsc/vitest/lint green. **Commit:** `feat(v9.1): store branches on recognition (assembly count, pattern save, complete/SRS)`

## Chunk 3: step/primitive UI + Review null-safety

**Files:** `StepStructure.tsx`, `StepAssemble.tsx`, `StepFeedback.tsx`, `StepReflect.tsx`, `primitives/ComparisonCard.tsx`, `primitives/Pattern5HChip.tsx`, `pages/Review.tsx`

- [ ] **ComparisonCard:** rewrite for `{fiveH,simpler,betterChoice}` — show both, highlight winner. Keep `show` gate.
- [ ] **StepStructure:** recognition banner (moment ✓ / 간결형 ✓ + cue); `Pattern5HChip` only when `pattern5h`; patternQuiz unchanged (dual text).
- [ ] **StepAssemble:** `blocksComplete`/counter use `payload.assembly.blocks.length` (spec I4).
- [ ] **StepFeedback:** `PatternNoteCard` only when `pattern5h` (else generic note markup); button label moment vs "확인".
- [ ] **StepReflect:** pattern-card hero + verb-family region gated on moment; simpler → recognition takeaway markup; after-choice/save only when moment.
- [ ] **Review.tsx:** Sentences chip — when `!r.isFiveHMoment`/`pattern5hId==null`, single "간결형" badge (no PATTERN_LABEL lookup, no triggerVerb).
- [ ] **Run** tsc/vitest/lint + `npm run build` green. **Commit:** `feat(v9.1): step/primitive UI branches + Review null-safe chip`

## Chunk 4: prompt rewrite + e2e + CLAUDE.md + verify

**Files:** `services/prompts.ts`, `api/chat.ts`/`dev-server.js` (model unchanged), `e2e/learn-flow.spec.ts`, `CLAUDE.md`

- [ ] **prompts.ts:** rewrite SYSTEM_PROMPT — recognition mandate (classify moment vs simpler; teach the cue; don't force), full new JSON shape, one moment + one simpler example, invert the "always pick closest pattern" rule (spec M3).
- [ ] **e2e learn-flow:** update step0 selectors for recognition banner/comparison; `schemaVersion` assertion 5→6; keep 3-block seed walkthrough.
- [ ] **CLAUDE.md:** 5형식 axis → recognition framing; SessionPayload/LearningRecord/flow updates.
- [ ] **Run full recipe:** `tsc -b` 0 · `vitest run` · `lint` · `build` · `test:e2e` 2 pass.
- [ ] Code-review whole branch; fold findings. PR → merge → memory.
